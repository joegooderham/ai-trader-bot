# ─────────────────────────────────────────────────────────────────────────────
# AI Trader Bot - Docker Compose
# ─────────────────────────────────────────────────────────────────────────────

services:

  forex-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-trader-bot
    restart: unless-stopped
    environment:
      - IG_API_KEY=${IG_API_KEY}
      - IG_ACCOUNT_ID=${IG_ACCOUNT_ID}
      - IG_USERNAME=${IG_USERNAME}
      - IG_PASSWORD=${IG_PASSWORD}
      - IG_ENVIRONMENT=${IG_ENVIRONMENT}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_BOT_SYS_TOKEN=${TELEGRAM_BOT_SYS_TOKEN}
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - DASHBOARD_CMD_TOKEN=${DASHBOARD_CMD_TOKEN}
    volumes:
      - ./data_store:/app/data_store
      - ./logs:/app/logs
      - ./config:/app/config
      - ./.git:/app/.git:ro
    depends_on:
      - mcp-server
    networks:
      - forex-network

  mcp-server:
    build:
      context: .
      dockerfile: Dockerfile.mcp
    container_name: ai-trader-mcp
    restart: unless-stopped
    environment:
      - IG_API_KEY=${IG_API_KEY}
      - IG_ACCOUNT_ID=${IG_ACCOUNT_ID}
      - IG_USERNAME=${IG_USERNAME}
      - IG_PASSWORD=${IG_PASSWORD}
      - IG_ENVIRONMENT=${IG_ENVIRONMENT}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_BOT_SYS_TOKEN=${TELEGRAM_BOT_SYS_TOKEN}
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    volumes:
      - ./data_store:/app/data_store
    ports:
      - "8090:8090"
    networks:
      - forex-network

  health-monitor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-trader-health
    restart: unless-stopped
    environment:
      - IG_API_KEY=${IG_API_KEY}
      - IG_ACCOUNT_ID=${IG_ACCOUNT_ID}
      - IG_USERNAME=${IG_USERNAME}
      - IG_PASSWORD=${IG_PASSWORD}
      - IG_ENVIRONMENT=${IG_ENVIRONMENT}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_BOT_SYS_TOKEN=${TELEGRAM_BOT_SYS_TOKEN}
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    command: ["python", "-m", "scripts.health_monitor"]
    depends_on:
      - forex-bot
      - mcp-server
    networks:
      - forex-network

  dashboard:
    build:
      context: ./dashboard
      dockerfile: Dockerfile
    container_name: ai-trader-dashboard
    restart: unless-stopped
    environment:
      - MCP_SERVER_URL=http://mcp-server:8090
      - DB_PATH=/app/data_store/trader.db
      - CONFIG_PATH=/app/config/config.yaml
      - WIKI_REPO_URL=https://github.com/joegooderham/ai-trader-bot.wiki.git
      - BOT_COMMAND_URL=http://forex-bot:8060
      - DASHBOARD_CMD_TOKEN=${DASHBOARD_CMD_TOKEN}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    volumes:
      - ./data_store:/app/data_store:ro
      - ./config:/app/config:ro
    ports:
      - "8050:8050"
    depends_on:
      - mcp-server
      - forex-bot
    networks:
      - forex-network

  # Cloudflare Tunnel — exposes dashboard at aitradefintech.com via HTTPS
  # Routes: aitradefintech.com → dashboard:8050
  # SSL is handled by Cloudflare edge — no certs to manage
  # Auth is handled by Cloudflare Access — no app-level auth code needed
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: ai-trader-tunnel
    restart: unless-stopped
    command: tunnel --no-autoupdate run --token ${CLOUDFLARE_TUNNEL_TOKEN}
    environment:
      - CLOUDFLARE_TUNNEL_TOKEN=${CLOUDFLARE_TUNNEL_TOKEN}
    depends_on:
      - dashboard
    networks:
      - forex-network

networks:
  forex-network:
    driver: bridge

volumes:
  data:
  logs: